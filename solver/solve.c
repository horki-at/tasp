#include "solver.ih"

void solve(Cities cities, size_t temp, size_t M, size_t m)
{
	// TODO
	printf("Solving TSP for cities with\ntemp = %zu\nM = %zu\nm = %zu\n",
				 temp, M, m);
	printf("For cities:\n");
}
